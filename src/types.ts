export interface NimiqxPool {
  pool_id: string;
  name: string;
  website: string;
  fee: number;
  payout_interval: number;
  payout_minimum: number;
  official_protocol: number;
  servers: string[];
}